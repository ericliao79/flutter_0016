import 'package:flutter/material.dart';

class Business extends StatelessWidget {

  Business({super.key}) {
    print("Business constructor");
  }

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Business'),
    );
  }
}
